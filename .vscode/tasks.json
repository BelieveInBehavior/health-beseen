{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "backend: stop docker server",
      "type": "shell",
      "command": "docker compose stop server",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    }
  ]
}
